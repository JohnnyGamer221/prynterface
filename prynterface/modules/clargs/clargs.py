# Parses command line arguments and returns a dict of the parsed data.

import sys


def parse_clargs():
    """@todo implement function.
    Should read sys.argv and return a dict of the parsed data.
    Output: {
        "flag": bool,
        "option": str,
        "argument": str
    }

    prynterface [ -h | -v | -f <file> | -c <command> | -i ] [-l <log_file>]

    no args: ui mode
    -h  help
    -v  version
    -f  file (execute commands as lines)
    -c  pass command as argument
    -i  interactive cli mode
    -l  log file

    """
    return Exception("Not implemented yet.")
